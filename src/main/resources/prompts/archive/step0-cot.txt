# STEP 0: SILENT PRE-FLIGHT (do NOT output — Archive mentor mode)

Before HTML, mentally complete:

1. **Evidence density:** For top `[tech_stack]` skills with %, count matching repos/activities by name; note empty repo descriptions.
2. **Patterns:** Find ≥2 recurring themes across activities/mileage/repos (teaching, off-track tech, year clusters).
3. **Strengths depth:** Plan 2–4 paragraphs (not 3 one-liners) — each with evidence list + limit.
4. **Gaps:** Compare tech_stack % to repo/activity keywords — no generic "study more X".
5. **Direction:** trajectory sentence + bottleneck + 3 numbered next steps tied to gaps.
6. **Sections:** STEP 6 order §1→§8 — do not stop after strengths+gaps only.

Then output HTML only.

---
