/* ═══ LAYOUT: 단일 칼럼 — "Typographic Editorial" (recommended theme: indigo) ═══
   Serif + sans mix, italic bio pull-quote, timeline dots. Document-style, print-friendly.
   Wrapper class: .layout-editorial (alias: .page-single)
   NOT a plain stack of generic .section-card — use structure below. */

.layout-editorial,
.page-single {
  background: #fff;
  min-height: 100%;
}

/* ── Top banner (editorial hero — NOT a flat doc header only) ── */
.editorial-hero {
  background: linear-gradient(120deg, #1e1b4b 0%, var(--primary) 60%, var(--secondary) 100%);
  padding: 48px 0 40px;
}
.editorial-hero-inner {
  max-width: 680px;
  margin: 0 auto;
  padding: 0 40px;
}
.editorial-school-badge {
  display: inline-block;
  padding: 3px 12px;
  border-radius: 4px;
  background: rgba(255, 255, 255, 0.12);
  color: #c7d2fe;
  font-size: 11px;
  font-weight: 600;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  margin-bottom: 16px;
  font-family: var(--font-ui);
}
.editorial-hero .hero-name,
.editorial-hero h1 {
  font-family: var(--font-serif);
  font-size: clamp(2rem, 5vw, 2.625rem);
  font-weight: 400;
  font-style: italic;
  color: #fff;
  margin: 0 0 6px;
  letter-spacing: -0.01em;
  line-height: 1.1;
}
.editorial-hero .hero-role {
  color: #a5b4fc;
  font-size: 17px;
  margin: 0;
  font-family: var(--font-ui);
  font-weight: 300;
  letter-spacing: 0.02em;
}

.editorial-body {
  max-width: 680px;
  margin: 0 auto;
  padding: 44px 40px 60px;
}

.editorial-bio {
  font-family: var(--font-serif);
  font-size: 15.5px;
  color: #1e1b4b;
  line-height: 1.85;
  border-left: 3px solid var(--secondary);
  padding-left: 20px;
  margin-bottom: 44px;
  font-style: italic;
}

.editorial-skills-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2px 36px;
  margin-bottom: 40px;
}

.editorial-project-row {
  padding-bottom: 20px;
  margin-bottom: 20px;
  border-bottom: 1px solid var(--track);
}
.editorial-project-row h3 {
  font-size: 15px;
  font-weight: 700;
  color: #1e1b4b;
  margin: 0 0 6px;
  font-family: var(--font-ui);
}
.editorial-project-meta {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  margin-bottom: 6px;
}
.editorial-project-period {
  font-size: 12px;
  color: var(--secondary);
  font-weight: 600;
}
.editorial-project-desc {
  font-size: 13.5px;
  color: #4b5563;
  line-height: 1.6;
  margin: 0 0 10px;
}

/* Timeline */
.editorial-timeline {
  position: relative;
  padding-left: 22px;
  margin-bottom: 40px;
}
.editorial-timeline::before {
  content: "";
  position: absolute;
  left: 6px;
  top: 4px;
  bottom: 4px;
  width: 1px;
  background: var(--track);
}
.editorial-timeline-item {
  position: relative;
  margin-bottom: 18px;
}
.editorial-timeline-dot {
  position: absolute;
  left: -19px;
  top: 5px;
  width: 7px;
  height: 7px;
  border-radius: 50%;
  background: #c7d2fe;
}
.editorial-timeline-item.is-award .editorial-timeline-dot {
  width: 10px;
  height: 10px;
  left: -20px;
  top: 4px;
  background: var(--primary);
  box-shadow: 0 0 0 3px var(--primary-soft);
}
.editorial-timeline-title {
  font-size: 14px;
  font-weight: 600;
  color: #1e1b4b;
  font-family: var(--font-ui);
}
.editorial-timeline-item.is-award .editorial-timeline-title {
  font-weight: 700;
  color: var(--primary);
}
.editorial-timeline-period {
  font-size: 11.5px;
  color: #94a3b8;
}
.editorial-timeline-desc {
  font-size: 13px;
  color: #6b7280;
  margin: 2px 0 0;
}

.editorial-contact {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  font-family: var(--font-ui);
}
.editorial-contact a {
  color: var(--primary);
  font-size: 13.5px;
  text-decoration: none;
  font-weight: 600;
}

@media (max-width: 640px) {
  .editorial-hero-inner,
  .editorial-body { padding-left: 20px; padding-right: 20px; }
  .editorial-skills-grid { grid-template-columns: 1fr; }
}

@media print {
  .editorial-hero {
    padding: 24px 0;
    -webkit-print-color-adjust: exact;
    print-color-adjust: exact;
  }
  .editorial-body { padding: 24px 20px; }
}

/*
<body class="layout-editorial">
  <header class="editorial-hero">
    <div class="editorial-hero-inner">
      <span class="editorial-school-badge">학과</span>
      <h1 class="hero-name">이름</h1>
      <p class="hero-role">직무</p>
    </div>
  </header>
  <div class="editorial-body">
    <p class="editorial-bio">...</p>
    <section><div class="section-label"><span>기술 스택</span></div>
      <div class="editorial-skills-grid"> skill-row × N </div></section>
    <section><div class="section-label"><span>프로젝트</span></div>
      <article class="editorial-project-row">...</article></section>
    <section><div class="section-label"><span>활동 &amp; 수상</span></div>
      <div class="editorial-timeline">
        <div class="editorial-timeline-item is-award">...</div>
      </div></section>
    <div class="section-label"><span>연락처</span></div>
    <div class="editorial-contact">...</div>
  </div>
</body>
*/
