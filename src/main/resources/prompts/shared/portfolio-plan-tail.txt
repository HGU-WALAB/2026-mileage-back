```

---

# OUTPUT FORMAT
Return **only** JSON. You may wrap the JSON in a single ```json fenced block **or** emit raw `{ ... }`.

Required top-level keys (use empty arrays/objects when unused):

- **layout_summary** (string, Korean): planned layout vs STEP 4 preference.
- **color_tokens** (object): include **page_background** (neutral hex), **primary_usage**, **soft_usage** (must say badges/tints only).
- **duplicate_activity_clusters** (array of objects): each has **titles_involved** (strings from STEP 2) and **merge_into_single_card** (boolean).
- **tech_stack_visualization** (array): each item has **skill_line_from_step2** (verbatim line including NN%) and **ui_pattern** (`horizontal_progress_bar` | `pill_with_percent` | `table_column`).
- **repo_rendering_notes** (array): each item has **repo_title** (verbatim) and **surface_fields** (array listing which sub-lines exist in STEP 2 to show—기간/팀/역할/기여).
- **strength_claims_with_evidence** (array): each item has **claim_ko** and **must_cite** (array of `"repo: …"` / `"activity: …"` references that exist in STEP 2).
- **sections** (array): objects with **id**, **title_ko**, **evidence_refs**, **omit_if_empty**.
- **fabrication_risks** (array of strings): where inference must be avoided.
- **layout_implementation** (string): `typographic_editorial` | `immersive_web` | `dark_dev_sidebar` | `bento_dashboard`
- **awards_and_certs_found** (array of strings): verbatim lines from STEP 2
- **achievements_section_required** (boolean)
- **density_mode** (string): `one_page_a4` | `unlimited`
- **secondary_items_to_hide** (array): project/activity titles to mark `.secondary-*` when density is one page
- **link_targets** (array): `{ "title", "url", "kind": "repo|activity|github_profile|email" }` from STEP 2 only
- **description_rewrite** (array): `{ "source_title", "rewritten_ko", "tech_cited": [] }` per RULE 10 — tags/original only
- **tech_evidence_density** (array): archive mentor — skill, pct, evidence_count, repo/activity names, interpretation_ko
- **patterns** (array): theme, citations[], reflective_question_ko
- **strengths_deep** (array): claim, evidence_names[], stack_link, limit_ko
- **gaps_contrast** (array): gap_ko, compared[], interpretation_ko
- **direction** (object): trajectory_ko, bottleneck_ko, next_steps[{ rank, action_ko, why_ko }]

No prose outside JSON (except optional single ```json fence pair).
