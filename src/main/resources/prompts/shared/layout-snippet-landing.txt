/* === LAYOUT: 랜딩 페이지 (MANDATORY when STEP 2 says 레이아웃: 랜딩 페이지) ===
   Do NOT render as a plain vertical stack of .section-card only — implement hero + nav + alternating sections + project grid. */

.site-nav {
  position: sticky; top: 0; z-index: 40;
  display: flex; flex-wrap: wrap; gap: 0.75rem 1.25rem;
  padding: 0.65rem 1.25rem;
  background: rgba(255, 255, 255, 0.92);
  border-bottom: 1px solid var(--border);
  backdrop-filter: blur(8px);
}
.site-nav a {
  font-size: 0.82rem; font-weight: 600; color: var(--muted);
  text-decoration: none; letter-spacing: 0.02em;
}
.site-nav a:hover { color: var(--primary); }

.hero {
  padding: 2.5rem 1.5rem 2rem;
  margin-bottom: 0;
  background: linear-gradient(135deg, var(--primary) 0%, var(--secondary, #4f46e5) 55%, #1e3a8a 100%);
  color: #ffffff;
  border-radius: 0;
}
.hero .hero-name { font-size: clamp(1.85rem, 4vw, 2.5rem); font-weight: 700; line-height: 1.15; margin: 0 0 0.35em; }
.hero .hero-role { font-size: 1.05rem; font-weight: 600; opacity: 0.95; margin: 0 0 0.5em; }
.hero .hero-tagline { font-size: 1rem; font-weight: 400; line-height: 1.5; max-width: 42rem; opacity: 0.92; margin: 0; }

main.landing-main { max-width: 960px; margin: 0 auto; padding: 0 1rem 2rem; }

.section-band {
  padding: 1.5rem 1.25rem;
  margin: 0 -1rem 0;
}
.section-band:nth-of-type(even) { background: var(--page-bg); }
.section-band:nth-of-type(odd) { background: #ffffff; border-top: 1px solid var(--border); border-bottom: 1px solid var(--border); }

.section-band .section-card {
  margin-bottom: 0;
  box-shadow: none;
  border: none;
  padding: 0;
  background: transparent;
}

.projects-featured {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: 1rem;
  margin-top: 0.75rem;
}
.projects-featured .project-card {
  background: var(--card-bg);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 1rem 1.1rem;
  border-left: 4px solid var(--primary);
  box-shadow: 0 6px 16px rgba(15, 23, 42, 0.06);
}
.projects-other .project-card { margin-bottom: 0.75rem; }

/* HTML skeleton (adapt IDs to your nav anchors):
<nav class="site-nav"><a href="#about">About</a><a href="#tech">Tech</a>...</nav>
<header class="hero section-card">...</header>
<main class="landing-main">
  <section id="about" class="section-band">...</section>
  <section id="projects" class="section-band"><div class="projects-featured">...</div></section>
</main>
*/
