/* ═══ LAYOUT: 랜딩 페이지 — "Immersive Web" (recommended theme: cyan) ═══
   Full-bleed gradient hero, sticky nav, alternating section backgrounds, 3-col project grid.
   Wrapper: .layout-immersive — MUST differ from .layout-editorial single column. */

.layout-immersive {
  background: #f0f9ff;
  min-height: 100%;
  font-family: var(--font-ui);
}

/* Sticky nav */
.immersive-nav,
.site-nav {
  position: sticky;
  top: 0;
  z-index: 50;
  background: rgba(240, 249, 255, 0.88);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
  border-bottom: 1px solid rgba(8, 145, 178, 0.15);
  padding: 0 32px;
  height: 48px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}
.immersive-nav .nav-brand {
  font-weight: 800;
  color: var(--primary);
  font-size: 15px;
  letter-spacing: -0.02em;
}
.immersive-nav .nav-links {
  display: flex;
  gap: 24px;
  font-size: 12.5px;
  color: #64748b;
  font-weight: 600;
}
.immersive-nav .nav-links a {
  color: inherit;
  text-decoration: none;
}
.immersive-nav .nav-links a:hover { color: var(--primary); }

/* Hero — full bleed */
.immersive-hero,
.hero {
  background: linear-gradient(160deg, #083344 0%, #0e7490 35%, var(--primary) 65%, var(--secondary) 100%);
  padding: 80px 32px 96px;
  text-align: center;
  position: relative;
  overflow: hidden;
  color: #fff;
}
.immersive-hero::before,
.immersive-hero::after {
  content: "";
  position: absolute;
  border-radius: 50%;
  border: 1px solid rgba(255, 255, 255, 0.05);
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  pointer-events: none;
}
.immersive-hero::before { width: 300px; height: 300px; }
.immersive-hero::after { width: 500px; height: 500px; }
.immersive-hero-inner {
  position: relative;
  z-index: 1;
  max-width: 640px;
  margin: 0 auto;
}
.immersive-hero .hero-school-badge {
  display: inline-block;
  padding: 5px 16px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.1);
  backdrop-filter: blur(4px);
  color: #cffafe;
  font-size: 11.5px;
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  margin-bottom: 24px;
  border: 1px solid rgba(255, 255, 255, 0.15);
}
.immersive-hero .hero-name,
.immersive-hero h1 {
  font-size: clamp(2.5rem, 6vw, 3.625rem);
  font-weight: 900;
  color: #fff;
  letter-spacing: -0.04em;
  margin: 0 0 10px;
  line-height: 1;
}
.immersive-hero .hero-role {
  color: var(--secondary);
  font-size: 22px;
  font-weight: 500;
  margin: 0 0 28px;
}
.immersive-hero .hero-tagline {
  color: rgba(255, 255, 255, 0.7);
  font-size: 15px;
  max-width: 500px;
  margin: 0 auto 36px;
  line-height: 1.8;
}
.immersive-hero-cta {
  display: flex;
  gap: 12px;
  justify-content: center;
  flex-wrap: wrap;
}
.immersive-hero-cta .btn-primary {
  padding: 11px 28px;
  background: #fff;
  color: var(--primary);
  border-radius: 8px;
  font-weight: 700;
  font-size: 14px;
  text-decoration: none;
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.15);
}
.immersive-hero-cta .btn-ghost {
  padding: 11px 28px;
  background: rgba(255, 255, 255, 0.12);
  color: #fff;
  border-radius: 8px;
  font-weight: 600;
  font-size: 14px;
  border: 1px solid rgba(255, 255, 255, 0.25);
  text-decoration: none;
}

/* Alternating bands */
.immersive-band,
.section-band {
  padding: 60px 32px;
}
.immersive-band--light {
  background: #fff;
}
.immersive-band--tint {
  background: var(--primary-soft);
}
.immersive-band-inner {
  max-width: 860px;
  margin: 0 auto;
}
.immersive-band-title {
  text-align: center;
  margin-bottom: 36px;
}
.immersive-band-title span {
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.16em;
  text-transform: uppercase;
  color: var(--primary);
}

.immersive-skills-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 4px 52px;
}
@media (max-width: 768px) {
  .immersive-skills-grid { grid-template-columns: 1fr 1fr; }
}

.projects-featured {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 14px;
}
@media (max-width: 900px) {
  .projects-featured { grid-template-columns: 1fr; }
}
.immersive-project-card,
.projects-featured .project-card {
  background: #fff;
  border-radius: 14px;
  padding: 22px 20px;
  border: 1px solid rgba(8, 145, 178, 0.15);
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.04);
}
.immersive-project-card.is-featured,
.projects-featured .project-card:first-child {
  border-color: var(--secondary);
  box-shadow: 0 8px 32px rgba(6, 182, 212, 0.15);
}
.immersive-project-card .card-accent {
  width: 32px;
  height: 3px;
  background: linear-gradient(90deg, var(--primary), var(--secondary));
  border-radius: 2px;
  margin-bottom: 16px;
}
.immersive-project-card h3 {
  font-size: 14px;
  font-weight: 800;
  color: #0c4a6e;
  margin-bottom: 8px;
  line-height: 1.3;
}
.immersive-project-card .project-period {
  font-size: 12px;
  color: #94a3b8;
  font-weight: 600;
  margin-bottom: 6px;
}
.immersive-project-card p {
  font-size: 12.5px;
  color: #64748b;
  line-height: 1.6;
  margin-bottom: 14px;
}

.immersive-activities-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}
@media (max-width: 640px) {
  .immersive-activities-grid { grid-template-columns: 1fr; }
}
.immersive-activity-card {
  padding: 16px 20px;
  border-radius: 12px;
  background: #f8fafc;
  border: 1px solid #e0f2fe;
}
.immersive-activity-card.is-award {
  background: linear-gradient(135deg, #0e7490, var(--secondary));
  border: none;
  color: #fff;
}
.immersive-activity-card.is-award .activity-title,
.immersive-activity-card.is-award .activity-desc {
  color: #fff;
}
.immersive-activity-card.is-award .activity-period {
  color: rgba(255, 255, 255, 0.65);
}
.immersive-activity-card .activity-title {
  font-weight: 700;
  font-size: 14px;
  color: #0c4a6e;
}
.immersive-activity-card .activity-period {
  font-size: 11.5px;
  color: #94a3b8;
  margin-top: 3px;
}
.immersive-activity-card .activity-desc {
  font-size: 12.5px;
  color: #64748b;
  margin-top: 7px;
  line-height: 1.5;
}

.immersive-footer-cta {
  background: linear-gradient(135deg, #083344, var(--primary));
  padding: 52px 32px;
  text-align: center;
  color: #fff;
}
.immersive-footer-cta h2 {
  color: #fff;
  font-weight: 700;
  font-size: 22px;
  margin-bottom: 8px;
}
.immersive-footer-cta p {
  color: rgba(255, 255, 255, 0.6);
  font-size: 14px;
  margin-bottom: 24px;
}
.immersive-footer-cta .footer-links {
  display: flex;
  gap: 20px;
  justify-content: center;
  flex-wrap: wrap;
  font-weight: 600;
  font-size: 14px;
  color: var(--secondary);
}

/*
<body class="layout-immersive">
  <nav class="immersive-nav site-nav">
    <span class="nav-brand">이름</span>
    <div class="nav-links"><a href="#tech">기술</a><a href="#projects">프로젝트</a>...</div>
  </nav>
  <header class="immersive-hero hero">...</header>
  <section id="tech" class="immersive-band immersive-band--light">...</section>
  <section id="projects" class="immersive-band immersive-band--tint">
    <div class="projects-featured">...</div>
  </section>
  <section class="immersive-band immersive-band--light">activities grid</section>
  <footer class="immersive-footer-cta">...</footer>
</body>
*/
