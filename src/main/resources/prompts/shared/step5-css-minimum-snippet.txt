<!--
  Shared primitives for all custom layouts. Map --primary/--secondary/--primary-soft to STEP 4 color theme.
  Pairing guide: 단일 칼럼→indigo | 랜딩→cyan | 사이드바→emerald | 카드 그리드→amber
-->
<style>
/* --- shared primitives (merge + keep when adding layout CSS) --- */
@import url("https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;500;600;700&family=Inter:wght@400;500;600;700&display=swap");

:root {
  --page-bg: #f8fafc;
  --card-bg: #ffffff;
  --text: #111827;
  --text-deep: #0f172a;
  --muted: #64748b;
  --primary: #4f46e5;
  --secondary: #818cf8;
  --primary-soft: #eef2ff;
  --track: #e0e7ff;
  --border: rgba(148, 163, 184, 0.35);
  --radius: 14px;
  --font-ui: "Noto Sans KR", "Inter", system-ui, sans-serif;
  --font-serif: Georgia, "Noto Serif KR", "Noto Sans KR", serif;
}

html, body {
  margin: 0;
  font-family: var(--font-ui);
  font-size: 15px;
  font-weight: 400;
  line-height: 1.55;
  background: var(--page-bg);
  color: var(--text);
}

h1, h2, h3, h4 {
  font-family: var(--font-ui);
  font-weight: 700;
  line-height: 1.25;
  color: var(--text-deep);
}

/* Skill bar (sidebar dark + all layouts) */
.skill-row { margin-bottom: 11px; }
.skill-meta {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  font-size: 12.5px;
  margin-bottom: 5px;
}
.skill-meta .skill-name { font-weight: 500; color: var(--text-deep); }
.skill-meta .skill-pct { font-weight: 600; opacity: 0.75; color: var(--primary); }
.skill-track {
  height: 5px;
  border-radius: 999px;
  background: var(--track);
  overflow: hidden;
}
.skill-fill {
  height: 100%;
  border-radius: 999px;
  background: linear-gradient(90deg, var(--primary), var(--secondary));
}

.tag-pill {
  display: inline-block;
  padding: 2px 9px;
  border-radius: 999px;
  font-size: 11px;
  font-weight: 600;
  background: var(--primary-soft);
  color: var(--primary);
  margin-right: 4px;
  margin-top: 3px;
}

.section-label {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 20px;
}
.section-label span {
  font-size: 10px;
  font-weight: 800;
  letter-spacing: 0.14em;
  text-transform: uppercase;
  color: var(--primary);
  flex-shrink: 0;
}
.section-label::after {
  content: "";
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, var(--track), transparent);
}

.achievements-highlight {
  background: var(--primary-soft);
  border: 2px solid var(--primary);
  border-radius: var(--radius);
  padding: 1rem 1.15rem;
  margin: 1rem 0 1.25rem;
}
.achievements-highlight h2 {
  color: var(--primary);
  font-size: 1rem;
  margin: 0 0 0.5rem;
}
.achievements-highlight ul { margin: 0; padding-left: 1.2rem; }
.achievements-highlight li { font-weight: 600; margin-bottom: 0.35rem; }

.contribution-missing {
  font-style: italic;
  color: var(--muted);
  font-size: 0.9em;
}

@media print {
  body { font-size: 11pt; line-height: 1.45; background: #fff !important; color: #000 !important; }
  a { color: #000 !important; text-decoration: underline; }
}
</style>
