# STEP 5: HTML REQUIREMENTS

The generated HTML MUST:
- be a **single HTML file** with embedded `<style>` (no external `.css` files). The `<head>` MUST include **both** (1) verbatim Google Fonts `<link rel="preconnect">` + `<link href="...Noto+Sans+KR...">` rows (see STEP 5-A / 5-B) **and** (2) `@import` for the same family at the top of `<style>`. HTML without `<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+KR` in `<head>` is **invalid**.
- include **`@import`** for those Google Fonts inside `<style>` **when** generating custom layouts (STEP 5-B), so fonts still load if `<link>` is stripped — `@import` must appear at the **top** of the stylesheet before other rules.
- When `[design_preferences]` is **missing or empty**, the prompt embeds the full **STEP 5-A default blue** fragment (verbatim `<style>` + body skeleton). Use it **exactly** — never replace it with improvised CSS/layout.
- support responsive layout and a complete **`@media print`** block (main column width/padding, readable font size, avoid awkward breaks inside cards — not only hiding chrome).
- use semantic HTML; avoid unnecessary JS frameworks
- maintain accessibility and recruiter readability

